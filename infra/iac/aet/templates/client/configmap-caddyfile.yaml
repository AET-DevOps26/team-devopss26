apiVersion: v1
kind: ConfigMap
metadata:
  name: client-caddyfile
  namespace: {{ .Values.global.namespace }}
data:
  Caddyfile: |
    :80 {
        # ── Dev routes (prefix-based, proxies cross-namespace to team-devopss26-dev) ──
        handle /dev/api/user/* {
            uri strip_prefix /dev
            reverse_proxy http://user-service.{{ .Values.global.namespace }}.svc.cluster.local:{{ index .Values.services "user-service" "appPort" }}
        }
        handle /dev/api/admin/* {
            uri strip_prefix /dev
            reverse_proxy http://admin-service.{{ .Values.global.namespace }}.svc.cluster.local:{{ index .Values.services "admin-service" "appPort" }}
        }
        handle /dev/api/checklist/* {
            uri strip_prefix /dev
            reverse_proxy http://checklist-service.{{ .Values.global.namespace }}.svc.cluster.local:{{ index .Values.services "checklist-service" "appPort" }}
        }
        handle /dev/api/calendar/* {
            uri strip_prefix /dev
            reverse_proxy http://calendar-service.{{ .Values.global.namespace }}.svc.cluster.local:{{ index .Values.services "calendar-service" "appPort" }}
        }
        handle /dev/api/note/* {
            uri strip_prefix /dev
            reverse_proxy http://note-service.{{ .Values.global.namespace }}.svc.cluster.local:{{ index .Values.services "note-service" "appPort" }}
        }
        handle /dev/api/genai/* {
            uri strip_prefix /dev
            reverse_proxy http://genai-service.{{ .Values.global.namespace }}.svc.cluster.local:{{ .Values.genaiService.appPort }}
        }
        handle /dev/* {
            uri strip_prefix /dev
            reverse_proxy http://client.{{ .Values.global.namespace }}.svc.cluster.local:80
        }

        # ── Prod routes ──
        handle /api/user/* {
            reverse_proxy http://user-service:{{ index .Values.services "user-service" "appPort" }}
        }
        handle /api/admin/* {
            reverse_proxy http://admin-service:{{ index .Values.services "admin-service" "appPort" }}
        }
        handle /api/checklist/* {
            reverse_proxy http://checklist-service:{{ index .Values.services "checklist-service" "appPort" }}
        }
        handle /api/calendar/* {
            reverse_proxy http://calendar-service:{{ index .Values.services "calendar-service" "appPort" }}
        }
        handle /api/note/* {
            reverse_proxy http://note-service:{{ index .Values.services "note-service" "appPort" }}
        }
        handle /api/genai/* {
            reverse_proxy http://genai-service:{{ .Values.genaiService.appPort }}
        }

        handle {
            root * /usr/share/caddy
            try_files {path} /index.html
            file_server
        }
    }