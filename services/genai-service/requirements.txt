fastapi==0.115.0
uvicorn[standard]==0.30.6
langchain==0.3.0
langchain-google-genai==2.0.0
langchain-groq==0.2.0
langchain-mistralai==0.2.0
langchain-cohere==0.3.0
# Local (self-hosted / offline) LLM option via Ollama — see the model=="local" branch in
# main.py's _build_chain(). Left commented out: the local model is not deployed, so this
# dependency is only needed when running against a local Ollama server.
# langchain-ollama==0.2.0
pydantic==2.9.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.29.0
PyJWT[crypto]==2.9.0
weaviate-client>=4.6.0
httpx>=0.27.0
prometheus-fastapi-instrumentator>=6.0.0
